---
phase: 01-p0-01-baseline-tag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - P0-01

must_haves:
  truths:
    - "git tag --list shows repatch-1.x-baseline pointing at the HEAD of main before any modernization commits"
    - "The tag is visible on the remote (origin) so the boss can inspect it"
    - "The working tree is clean — no uncommitted changes — at the tagged commit"
  artifacts:
    - path: "(git object)"
      provides: "Annotated tag repatch-1.x-baseline at commit 5e74277"
      contains: "repatch-1.x-baseline"
  key_links:
    - from: "local tag repatch-1.x-baseline"
      to: "origin/refs/tags/repatch-1.x-baseline"
      via: "git push origin repatch-1.x-baseline"
      pattern: "repatch-1.x-baseline"
---

<objective>
Freeze the pre-modernization state of the RePatch 1.x codebase by creating an annotated
git tag `repatch-1.x-baseline` on the current HEAD of `main` and pushing it to the remote
so the boss has an unambiguous rollback point visible on the server.

Purpose: Every subsequent modernization branch diverges from a clearly named, immutable
checkpoint. If anything goes wrong at any phase, `git checkout repatch-1.x-baseline`
returns to a known-good state.

Output: Annotated git tag `repatch-1.x-baseline` pushed to origin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/adamhamou/Documents/Work/Repatch_Modernization/.planning/PROJECT.md
@/Users/adamhamou/Documents/Work/Repatch_Modernization/.planning/ROADMAP.md
@/Users/adamhamou/Documents/Work/Repatch_Modernization/.planning/STATE.md

<!-- Branch naming convention (from CLAUDE.md):
     task/<wbs-id>-<short-description>  →  task/p0-01-baseline-tag

     Phase 1 success criteria (ROADMAP.md Phase 1):
       1. git tag --list shows repatch-1.x-baseline on last commit of pre-modernization main.
       2. Tag pushed to origin, visible remotely.
       3. Repo clean — no uncommitted changes.

     Current repo state (confirmed at planning time):
       - Branch: main
       - HEAD: 5e74277 (docs: create roadmap)
       - Uncommitted changes: none (only untracked files: .claude/, RePatch/, RePatch_2_0.pdf)
       - Existing tags: none
       - Configured remotes: NONE — see Task 2 for handling
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify clean state and create annotated tag on main HEAD</name>
  <files>(no files modified — git metadata only)</files>

  <read_first>
    - Run `git status` to confirm no staged or unstaged modifications exist.
    - Run `git branch --show-current` to confirm the active branch is `main`.
    - Run `git log --oneline -1` to record the exact commit SHA that will be tagged.
    - Run `git tag --list` to confirm `repatch-1.x-baseline` does not already exist.
  </read_first>

  <action>
    Execute the following commands in order from the repo root
    `/Users/adamhamou/Documents/Work/Repatch_Modernization`:

    1. Confirm clean state (untracked files are acceptable; staged/unstaged changes are not):
       ```
       git status
       ```
       Expected output must NOT include lines beginning with "Changes to be committed:"
       or "Changes not staged for commit:". Untracked files are fine.
       If unexpected staged/unstaged changes are present, STOP and report — do not tag.

    2. Confirm branch is main:
       ```
       git branch --show-current
       ```
       Must output exactly: `main`

    3. Record the HEAD SHA (for confirmation):
       ```
       git log --oneline -1
       ```

    4. Create the annotated tag:
       ```
       git tag -a repatch-1.x-baseline -m "RePatch 1.x baseline — pre-modernization snapshot. Tagged before any IntelliJ Platform Gradle Plugin 2.x migration work."
       ```

    5. Confirm the tag was created and points at HEAD:
       ```
       git show repatch-1.x-baseline --stat
       ```
       Output must begin with "tag repatch-1.x-baseline" and show the annotator message.
  </action>

  <verify>
    <automated>git -C /Users/adamhamou/Documents/Work/Repatch_Modernization tag --list | grep "^repatch-1.x-baseline$"</automated>
  </verify>

  <acceptance_criteria>
    - `git tag --list | grep "^repatch-1.x-baseline$"` prints exactly `repatch-1.x-baseline`.
    - `git show repatch-1.x-baseline` output begins with `tag repatch-1.x-baseline` (annotated, not lightweight).
    - `git rev-list repatch-1.x-baseline^0 --max-count=1` matches the SHA from step 3 (tags the same commit that was HEAD of main before any modernization).
    - `git status` shows no staged or unstaged modifications (untracked files permitted).
  </acceptance_criteria>

  <done>
    Annotated tag `repatch-1.x-baseline` exists locally, pointing at the final
    pre-modernization commit on `main`, with no working-tree modifications.
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Configure git remote and push tag to origin</name>

  <read_first>
    - Run `git remote -v` to check whether an `origin` remote is already configured.
    - This was confirmed at planning time to have NO remote — the user must supply the
      GitHub URL before the push can proceed.
  </read_first>

  <action>
    Step A — Check for remote:
    ```
    git remote -v
    ```

    If `origin` is already listed, skip Step B and proceed directly to Step C.

    Step B (HUMAN ACTION REQUIRED if no remote exists):
    Git has no remote configured. The executor cannot know the GitHub repository URL.

    The user must run ONE of the following (substituting the actual URL):
    ```
    # HTTPS (recommended for most setups):
    git remote add origin https://github.com/<your-org>/<your-repo>.git

    # SSH:
    git remote add origin git@github.com:<your-org>/<your-repo>.git
    ```

    After adding the remote, confirm with:
    ```
    git remote -v
    ```
    Expected output:
    ```
    origin  https://github.com/<your-org>/<your-repo>.git (fetch)
    origin  https://github.com/<your-org>/<your-repo>.git (push)
    ```

    Step C — Push the tag to origin:
    ```
    git push origin repatch-1.x-baseline
    ```
    Expected output contains: `* [new tag]  repatch-1.x-baseline -> repatch-1.x-baseline`

    Step D — Verify the tag is visible on the remote:
    ```
    git ls-remote --tags origin | grep repatch-1.x-baseline
    ```
    Expected output contains: `refs/tags/repatch-1.x-baseline`
  </action>

  <how-to-verify>
    After completing the steps above:

    1. Run: `git ls-remote --tags origin | grep repatch-1.x-baseline`
       Must print a line containing `refs/tags/repatch-1.x-baseline`.

    2. Open the GitHub repository in a browser and navigate to:
       Releases → Tags (or Code → Tags)
       Confirm `repatch-1.x-baseline` appears in the tag list.

    3. The boss can now verify by visiting the repository URL and viewing the Tags page.
  </how-to-verify>

  <resume-signal>
    Type "pushed" once `git ls-remote --tags origin | grep repatch-1.x-baseline`
    returns output and the tag is visible on GitHub. If the remote did not exist and you
    added it, also confirm the remote URL is correct.
  </resume-signal>

  <acceptance_criteria>
    - `git remote -v` shows `origin` pointing at the correct GitHub repository URL (fetch + push).
    - `git ls-remote --tags origin | grep repatch-1.x-baseline` returns a line containing `refs/tags/repatch-1.x-baseline`.
    - Visiting GitHub repository Tags page shows `repatch-1.x-baseline` linked to commit matching local `git rev-list repatch-1.x-baseline^0 --max-count=1`.
  </acceptance_criteria>

  <done>
    Tag `repatch-1.x-baseline` is visible on the remote. Boss can navigate to
    the GitHub Tags page and confirm the freeze point independently.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → GitHub remote | Tag pushed over HTTPS/SSH to remote; credentials required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | git tag creation | accept | Annotated tag includes tagger identity and timestamp; any re-tagging is visible in git reflog |
| T-01-02 | Repudiation | tag push to origin | accept | GitHub records push actor in audit log; low-value operation (no secrets, no code changes) |
| T-01-03 | Information Disclosure | git remote add (HTTPS URL) | accept | URL is a public GitHub repo; no secrets in the remote URL itself |
</threat_model>

<verification>
Run all of the following from the repo root after both tasks complete:

```bash
# 1. Tag exists locally as an annotated tag
git show repatch-1.x-baseline | head -5
# Expected first line: "tag repatch-1.x-baseline"

# 2. Tag points at the pre-modernization main HEAD
git rev-list repatch-1.x-baseline^0 --max-count=1
# Must match the SHA recorded in Task 1 step 3

# 3. Tag is on the remote
git ls-remote --tags origin | grep repatch-1.x-baseline
# Must return: <sha>   refs/tags/repatch-1.x-baseline

# 4. Working tree is clean
git status
# Must NOT include "Changes to be committed" or "Changes not staged for commit"

# 5. Currently on main
git branch --show-current
# Must output: main
```
</verification>

<success_criteria>
1. `git tag --list | grep "^repatch-1.x-baseline$"` prints `repatch-1.x-baseline`.
2. `git ls-remote --tags origin | grep repatch-1.x-baseline` returns a line with `refs/tags/repatch-1.x-baseline`.
3. `git status` shows no staged or unstaged modifications.
4. Boss can navigate to the GitHub repository Tags page and see `repatch-1.x-baseline` as the earliest tag, linked to the last pre-modernization commit.
</success_criteria>

<output>
After completion, create `.planning/phases/01-p0-01-baseline-tag/01-01-SUMMARY.md`
following the template at `$HOME/.claude/get-shit-done/templates/summary.md`.

Key facts to record in the summary:
- Exact SHA of the tagged commit
- Remote URL added (if it was missing and the user supplied it)
- Confirmation that the tag is visible on GitHub
</output>
